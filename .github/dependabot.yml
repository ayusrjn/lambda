version: 2
updates:
  - package-ecosystem: "pip" # Use "pip" for requirements.txt or pyproject.toml
    directory: "/"           # Root directory
    schedule:
      interval: "daily"      # Options: daily, weekly, monthly
    open-pull-requests-limit: 10 # Prevent PR clutter
